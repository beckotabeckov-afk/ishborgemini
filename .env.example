# .env.example
GEMINI_API_KEY=
YANDEX_API_KEY=
# Path to service account JSON for Google Cloud TTS if using file,
# OR credentials in environment variables depending on deployment.
GOOGLE_APPLICATION_CREDENTIALS=
